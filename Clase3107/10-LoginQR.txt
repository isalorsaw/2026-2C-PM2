 {/* 🔘 BOTÓN DE LOGIN */}
            <TouchableOpacity 
              style={[styles.btn, botonDeshabilitado && styles.btnDisabled]} 
              onPress={handleLogin} 
              activeOpacity={0.85}
              disabled={botonDeshabilitado}
            >
              {loading ? (
                <ActivityIndicator color="#fff" size="small" />
              ) : (
                <Text style={styles.btnText}>
                  {conexionBD.estado === 'conectado' ? 'Iniciar Sesión' : 'Sin conexión'}
                </Text>
              )}
            </TouchableOpacity>

               {/* 📱 NUEVO BOTÓN: INGRESAR CON QR */}
            <TouchableOpacity 
              style={styles.btnQR} 
              onPress={irALoginQR}
              activeOpacity={0.7}
              disabled={botonDeshabilitado}
            >
              <Text style={styles.btnQRText}>🔲 Ingresar con Código QR</Text>
            </TouchableOpacity>
			
			
			 // 👇 AGREGAR ESTOS ESTILOS PARA EL BOTÓN QR
  btnQR: {
    backgroundColor: 'transparent',
    borderWidth: 2,
    borderColor: '#667eea',
    paddingVertical: 14,
    borderRadius: 12,
    alignItems: 'center',
    marginTop: 15,
  },
  btnQRText: {
    color: '#667eea',
    fontSize: 16,
    fontWeight: '700',
  },

  linkContainer: {
    marginTop: 20,
    alignItems: 'center',
  },